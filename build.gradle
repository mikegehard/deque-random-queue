apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'org.testng', name: 'testng', version: '6.8.8'
    compile files('../libraries/stdlib.jar')
}

test {
    useTestNG()

    testLogging {
        exceptionFormat "full"
    }
}
